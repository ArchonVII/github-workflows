- **Per-PR example callers gain concurrency-cancel; anomaly stub gains a path filter.** Nine `examples/` stubs (anomaly-triage, changelog-fragment, repo-update-log-fragment, pr-policy, branch-naming, semantic-pr-title, auto-merge-dependabot, dependency-review, doc-policy-lint) now cancel superseded runs on the same PR, and the anomaly-triage stub only triggers when `.archon/anomalies-thispr.md` is in the PR diff. Mirrors repo-template#140. (#101)
