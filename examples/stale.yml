# Copy to `.github/workflows/stale.yml`. Runs daily at 03:00 UTC.

name: Stale

on:
  schedule:
    - cron: "0 3 * * *"
  workflow_dispatch:

permissions:
  issues: write
  pull-requests: write

jobs:
  stale:
    uses: ArchonVII/github-workflows/.github/workflows/stale.yml@v1
    # with:
    #   days-before-stale: 60
    #   days-before-close: 14
    #   exempt-issue-labels: 'pinned,roadmap,blocked'
