# Copy to `.github/workflows/lock-threads.yml`. Runs daily at 04:00 UTC.

name: Lock Threads

on:
  schedule:
    - cron: "0 4 * * *"
  workflow_dispatch:

permissions:
  issues: write
  pull-requests: write

jobs:
  lock:
    uses: ArchonVII/github-workflows/.github/workflows/lock-threads.yml@v1
    # with:
    #   issue-inactive-days: 90
    #   pr-inactive-days: 30
