# Copy this file to `.github/workflows/labeler.yml` and put your path-glob
# config at `.github/labeler.yml` (or wherever configuration-path points).

name: Labeler

on:
  pull_request_target:
    types: [opened, synchronize, reopened]

permissions:
  contents: read
  pull-requests: write

jobs:
  label:
    uses: ArchonVII/github-workflows/.github/workflows/labeler.yml@v1
    # with:
    #   configuration-path: '.github/labeler.yml'
    #   sync-labels: true
